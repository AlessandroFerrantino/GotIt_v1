import 'package:flutter/material.dart';

class AccountShopPage extends StatelessWidget {
  const AccountShopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.yellow,
    );
  }
}
