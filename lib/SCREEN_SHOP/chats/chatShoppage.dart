import 'package:flutter/material.dart';

class MessageShopPage extends StatelessWidget {
  const MessageShopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
